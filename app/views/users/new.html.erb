<h2><%= t('txt.views.users.new.header') %></h2>

<%= error_messages_for @user %>

<%= form_for(@user, :url => users_path(:lang => I18n.locale), :html => {:class => "form"}) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
<% end %>

<%= content_for :sub_navigation do %>
  <ul>
    <li><%= back_link users_path(:lang => I18n.locale) %></li>
  </ul>
<% end %>
