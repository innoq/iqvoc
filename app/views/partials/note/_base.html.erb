<% note = concept.note_for_class(klass) %>
<h4><%= t("txt.partials.note.#{klass.name.to_relation_name}") %></h4>
<div class="relation-body">
  <span><%= note ? linkify(note) : t("txt.partials.note.missing") %></span>
</div>