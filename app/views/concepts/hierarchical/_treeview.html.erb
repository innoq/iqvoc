<%
  hcpath = hierarchical_concepts_path(:lang => @active_language,
      :format => :json, :pref_label_lang => params[:pref_label_lang],
      :published => params[:published])
%>
<ul class="treeview"
    data-remote-settings='{"url":"<%= hcpath %>", "root":"<%= root %>"}'>
</ul>
