<%= content_for :head do -%>
  <link href="<%= url_for(request.query_parameters.merge(:format => :ttl, :lang => nil, :only_path => false)) %>" type="text/turtle" rel="alternate" title=""/>
  <link href="<%= url_for(request.query_parameters.merge(:format => :rdf, :lang => nil, :only_path => false))  %>" type="application/rdf+xml" rel="alternate" title=""/>
<%- end -%>