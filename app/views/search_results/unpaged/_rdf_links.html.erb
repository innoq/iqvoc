<%= url_for(request.query_parameters.merge(:format => nil, :lang => nil, :only_path => false)) %>
(<%= link_to "Turtle", url_for(request.query_parameters.merge(:format => :ttl, :lang => nil, :only_path => false)) %>, <%= link_to "RDF/XML", url_for(request.query_parameters.merge(:format => :rdf, :lang => nil, :only_path => false)) %>)
